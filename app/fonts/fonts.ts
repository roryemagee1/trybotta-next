import { MuseoModerno } from 'next/font/google';

export const museoModerno = MuseoModerno({ subsets: ['latin'] });

// export const lusitana = Lusitana({ subsets: ['latin'], weight: ['400', '700'] })